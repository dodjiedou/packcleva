{% extends "dashboards/layout.html.twig" %}

{% block title %}
    {% if beneficiaire is not null %}
        {{ beneficiaire.name|capitalize }}
    {% endif %}
{% endblock %}

{% block stylesheets %}
    <!-- Vendor CSS Files -->
    <link href="{{ asset("defaults/assets/vendor/animate.css/animate.min.css") }}" rel="stylesheet">
    <link href="{{ asset("defaults/assets/vendor/aos/aos.css") }}" rel="stylesheet">
    <link href="{{ asset("defaults/assets/vendor/bootstrap/css/bootstrap.min.css") }}" rel="stylesheet">
    <link href="{{ asset("defaults/assets/vendor/bootstrap-icons/bootstrap-icons.css") }}" rel="stylesheet">
    <link href="{{ asset("defaults/assets/vendor/boxicons/css/boxicons.min.css") }}" rel="stylesheet">
    <link href="{{ asset("defaults/assets/vendor/glightbox/css/glightbox.min.css") }}" rel="stylesheet">
    {# <link href="{{ asset("defaults/assets/vendor/swiper/swiper-bundle.min.css") }}" rel="stylesheet"> #}

    <!-- Template Main CSS File -->
    <link href="{{ asset("defaults/assets/css/style.css") }}" rel="stylesheet">
{% endblock %}

{% block javascripts %}
    <!-- Template Main JS File -->
    <script src="{{ asset("defaults/assets/js/main.js") }}"></script>
    <!-- Vendor JS Files -->
    <script src="{{ asset("defaults/assets/vendor/aos/aos.js") }}"></script>
    <script src="{{ asset("defaults/assets/vendor/bootstrap/js/bootstrap.bundle.min.js") }}"></script>
    <script src="{{ asset("defaults/assets/vendor/glightbox/js/glightbox.min.js") }}"></script>
    <script src="{{ asset("defaults/assets/vendor/isotope-layout/isotope.pkgd.min.js") }}"></script>
    {# <script src="{{ asset("defaults/assets/vendor/php-email-form/validate.js") }}"></script> #}
    {# <script src="{{ asset("defaults/assets/vendor/swiper/swiper-bundle.min.js") }}"></script> #}
    {# <script src="{{ asset("defaults/assets/vendor/waypoints/noframework.waypoints.js") }}"></script> #}
{% endblock %}

{% block main %}

    <!-- Content Header (Page header) -->
    <div class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1 class="m-0">{{ ("services category"|trans|capitalize) }}</h1>
          </div><!-- /.col -->
          <div class="col-sm-6">
            <ol class="breadcrumb float-sm-right">
              <li class="breadcrumb-item"><a class="text-danger" href="{{ path("dashboard_index") }}">{{ ("dashboard"|capitalize) }}</a></li>
              <li class="breadcrumb-item"><a class="text-danger" href="{{ path("beneficiaire_index") }}">{{ ("bénéficiaire"|trans|capitalize) }}</a></li>
              <li class="breadcrumb-item active"><a class="text-danger" href="{{ path("beneficiaire_show",{'beneficiaire':beneficiaire, 'slug':beneficiaire.slug})}}">{{ ("show"|trans|capitalize) }}</a></li>
            </ol>
          </div><!-- /.col -->
        </div><!-- /.row -->
      </div><!-- /.container-fluid -->
    </div>
    <!-- /.content-header -->

    <!-- Main content -->
    <section class="content">
        <div class="container-fluid">
        
        <div class="row">
          <div class="col-12">
            <div class="card">
              <div class="card-header">
                <h3 class="card-title">
                  <i class="fas fa-text-width"></i>
                  {{ beneficiaire.name|trans|capitalize }}
                </h3>
              </div>
              <!-- /.card-header -->
                <div class="card-body">
                    {# <div class="row">

                        <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
                            <div class="carousel-inner">
                                {% for slide in beneficiaire.slides %}

                                <div class="carousel-item card active">
                                    <img width="1920" height="500" class="d-block w-100 card-img-top" src="{{ vich_uploader_asset(slide.image, 'file') }}" alt="{{ slide.name }}">
                                    <div class="card-img-overlay d-flex flex-column justify-content-center">
                                        <h5 class="card-title text-white mt-5 pt-2">{{ slide.name[:50] }}</h5>
                                        <p class="card-text pb-2 pt-1 text-white">{{ slide.description[:247]|raw }}&#8230;</p>
                                        <a href="#" class="text-white">{{("read more"|trans|capitalize)}}</a>
                                    </div>
                                </div>

                                {% else %}

                                <div class="carousel-item card active">
                                    <img width="1920" height="500" class="d-block w-100 card-img-top" src="{{ asset("defaults/assets/img/slide/slide-1.jpg") }}" alt="First slide">
                                    <div class="card-img-overlay d-flex flex-column justify-content-center text-center">
                                        <h5 class="card-title text-white mt-5 pt-2">Card Title</h5>
                                        <p class="card-text pb-2 pt-1 text-white">
                                        Lorem ipsum dolor sit amet, <br>
                                        consectetur adipisicing elit <br>
                                        sed do eiusmod tempor.
                                        </p>
                                        <a href="#" class="text-white">Last update 15 hours ago</a>
                                    </div>
                                </div>
                                <div class="carousel-item card">
                                    <img width="1920" height="500" class="d-block w-100 card-img-top" src="{{ asset("defaults/assets/img/slide/slide-2.jpg") }}" alt="Second slide">
                                    <div class="card-img-overlay d-flex flex-column justify-content-center text-center">
                                        <h5 class="card-title text-white mt-5 pt-2">Card Title</h5>
                                        <p class="card-text pb-2 pt-1 text-white">
                                        Lorem ipsum dolor sit amet, <br>
                                        consectetur adipisicing elit <br>
                                        sed do eiusmod tempor.
                                        </p>
                                        <a href="#" class="text-white">Last update 15 hours ago</a>
                                    </div>
                                </div>
                                <div class="carousel-item card">
                                    <img width="1920" height="500" class="d-block w-100 card-img-top" src="{{ asset("defaults/assets/img/slide/slide-3.jpg") }}" alt="Third slide">
                                    <div class="card-img-overlay d-flex flex-column text-center justify-content-center">
                                        <h5 class="card-title text-white mt-5 pt-2">Card Title</h5>
                                        <p class="card-text pb-2 pt-1 text-white">
                                        Lorem ipsum dolor sit amet, <br>
                                        consectetur adipisicing elit <br>
                                        sed do eiusmod tempor.
                                        </p>
                                        <a href="#" class="text-white">Last update 15 hours ago</a>
                                    </div>
                                </div>

                                {% endfor %}
                            </div>
                            <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
                            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                            <span class="sr-only">Previous</span>
                            </a>
                            <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
                            <span class="carousel-control-next-icon" aria-hidden="true"></span>
                            <span class="sr-only">Next</span>
                            </a>
                        </div>
                        
                    </div> #}
              
                    <blockquote>
                        <dl class="row">
                            <dt class="col-4">{{("nom"|trans|capitalize)}}</dt>
                            <dd class="col-8">{{ beneficiaire.nom|trans|capitalize }}.</dd>
                            <dt class="col-4">{{("prénoms"|trans|capitalize)}}</dt>
                            <dd class="col-8">{{ beneficiaire.prenom|trans|capitalize }}.</dd>
                            <dt class="col-4">{{("date de naissance"|trans|capitalize)}}</dt>
                            <dd class="col-8">
                                {{ beneficiaire.dateNaissance ? beneficiaire.dateNaissance|date('Y-m-d H:i:s') : '' }}
                            </dd>
                            <dt class="col-4">{{("téléphone"|trans|capitalize)}}</dt>
                            <dd class="col-8">{{ beneficiaire.telephone|trans|capitalize }}.</dd>
                            <dt class="col-4">{{("email"|trans|capitalize)}}</dt>
                            <dd class="col-8">{{ beneficiaire.email|trans|capitalize }}.</dd>
                            <dt class="col-4">{{("description"|trans|capitalize)}}</dt>
                            <dd class="col-8">{{ beneficiaire.description|trans|capitalize|raw }}.</dd>
                            <dt class="col-4">{{("numéro"|trans|capitalize)}}</dt>
                            <dd class="col-8">{{ beneficiaire.numero|trans|capitalize }}.</dd>
                            <dt class="col-4">{{("sexe"|trans|capitalize)}}</dt>
                            <dd class="col-8">{{ beneficiaire.sexe|trans|capitalize }}.</dd>
                            <dt class="col-4">{{("classe"|trans|capitalize)}}</dt>
                            <dd class="col-8">{{ beneficiaire.classe|trans|capitalize }}.</dd>
                            <dt class="col-4">{{("religion"|trans|capitalize)}}</dt>
                            <dd class="col-8">{{ beneficiaire.religion|trans|capitalize|raw }}.</dd>
                            <dt class="col-4">{{("tuteur"|trans|capitalize)}}</dt>
                            <dd class="col-8">{{ beneficiaire.tuteur|trans|capitalize|raw }}.</dd>
                            <dt class="col-4">{{("village"|trans|capitalize)}}</dt>
                            <dd class="col-8">{{ beneficiaire.village|trans|capitalize|raw }}.</dd>
                            <dt class="col-4">{{("rue"|trans|capitalize)}}</dt>
                            <dd class="col-8">{{ beneficiaire.rue|trans|capitalize|raw }}.</dd>
                            <dt class="col-4">{{("rangOccupe"|trans|capitalize)}}</dt>
                            <dd class="col-8">{{ beneficiaire.rangOccupe|trans|capitalize|raw }}.</dd>
                            <dt class="col-4">{{("classeCde"|trans|capitalize)}}</dt>
                            <dd class="col-8">{{ beneficiaire.classeCde|trans|capitalize|raw }}.</dd>
                        </dl>
                        <small>{{ beneficiaire.author }}, <cite title="Source Title">{{ beneficiaire.name }}</cite></small>
                    </blockquote>
                    <div>
                        <div class="col-4 btn-group btn-block">
                            <a class="btn btn-outline-success" href="{{ path('beneficiaire_index') }}">
                                <i class="fa fa-undo"></i> {{("back to list"|trans|capitalize)}}
                            </a>

                            <a class="btn btn-outline-warning" href="{{ path('beneficiaire_edit', {'slug': beneficiaire.slug}) }}">
                                <i class="fas fa-edit"></i> {{("modifier"|trans|capitalize)}}
                            </a>

                            {{ include('dashboards/beneficiaire/_delete_form.html.twig') }}
                        </div>
                    </div>
              </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->
          </div>
          <!-- ./col -->

        </div>
        <!-- /.card-body -->
        </div>
                    <!-- /.card -->
        <!-- /.row -->

        </div>
    </section>

{% endblock %}