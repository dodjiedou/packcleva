{% extends 'dashboards/layout.html.twig' %}

{% block title %}{{("Modifier Beneficiaire"|trans|upper)}}{% endblock %}

{% block javascripts %}
    <!-- jquery-validation -->
    <script src="{{asset("dashboards/plugins/jquery-validation/jquery.validate.min.js")}}"></script>
    <script src="{{asset("dashboards/plugins/jquery-validation/additional-methods.min.js")}}"></script>

    {# <script>
        $(function () {
            $.validator.setDefaults({
                submitHandler: function () {
                alert( "Formulaire soumis avec succès!" );
                }
            });
            $('#quickForm').validate({
                rules: {
                    name: {
                        required: true,
                        name: true,
                    },
                    description: {
                        required: true,
                        minlength: 15
                    }
                },
                messages: {
                    name: {
                        required: "veuillez entrer le nom de l'enseigne",
                        name: "veuillez entrer un nom d'enseigne valide"
                    },
                    description: {
                        required: "Veuillez fournir une description",
                        minlength: "Votre description doit comporter au moins 15 caractères"
                    }
                },
                errorElement: 'span',
                errorPlacement: function (error, element) {
                    error.addClass('invalid-feedback');
                    element.closest('.form-group').append(error);
                },
                highlight: function (element, errorClass, validClass) {
                    $(element).addClass('is-invalid');
                },
                unhighlight: function (element, errorClass, validClass) {
                    $(element).removeClass('is-invalid');
                }
            });
        });
    </script> #}
{% endblock %}

{% block main %}

    <!-- Content Header (Page header) -->
    <section class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1>{{("modifier"|trans|capitalize)}} <b>{{("bénéficiaire"|trans|capitalize)}}</b></h1>
          </div>
          <div class="col-sm-6">
            <ol class="breadcrumb float-sm-right">
              <li class="breadcrumb-item"><a class="text-danger" href="{{ path("dashboard_index") }}">{{ ("dashboard"|capitalize) }}</a></li>
              <li class="breadcrumb-item"><a class="text-danger" href="{{ path("beneficiaire_index") }}">{{ ("categories services"|capitalize) }}</a></li>
              <li class="breadcrumb-item active"><a class="text-danger" href="{{ path("beneficiaire_edit",{"slug":beneficiaire.slug}) }}">{{ ("modifier"|trans|capitalize) }}</a></li>
            </ol>
          </div>
        </div>
      </div><!-- /.container-fluid -->
    </section>

    <!-- Main content -->
    <section class="content">
        <div class="container-fluid">
            <div class="row">
            <!-- left column -->
                <div class="col-md-12">
                    <!-- jquery validation -->
                    <div class="card card-primary">
                        <div class="card-header">
                            <h3 class="card-title">{{("modifier"|trans|capitalize)}} <small>{{("bénéficiaire"|trans|capitalize)}}</small></h3>
                        </div>
                        <!-- /.card-header -->
                        <!-- form start -->
                        {% set beneficiaire_edit_new = "beneficiaire_edit" %}

                        {{ include('dashboards/beneficiaire/_form.html.twig', {'button_label': 'Update','beneficiaire_edit_new':beneficiaire_edit_new, 'slug':beneficiaire.slug}) }}
                    </div>
                    <div class="row">
                        <a class="btn pr-3 btn-outline-success col-2" href="{{ path('beneficiaire_index') }}">
                            <i class="fa fa-undo"></i> {{("back to list"|trans|capitalize)}}
                        </a>
                        <div class="col-9"></div>
                        <div class="col-1 pr-3 float-right" >
                            {{ include('dashboards/beneficiaire/_delete_form.html.twig',{'beneficiaire':beneficiaire}) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </section>
    
{% endblock %}
